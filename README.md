## How to use Student Study Helper

1. Go into .env file and insert your Gemini API key into "GEMINI_API_KEY".
2. Run "pip install -r requirements.txt" in your console".
3. Run the app by typing "python app.py" in your console.
4. Navigate to http://127.0.0.1:5000/ to access the landing page.
